<%= render 'sidebar', book: @book %>

<h1>Books</h1>
  <div>Title</div>
  <div>Opinion</div>
  <% @books.each do |book| %>
  <div>
    <%= image_tag book.get_image %>
    <%= link_to book.title, book_path(book.id) %>
    <%= book.body %>
  </div>
  <% end %>
  