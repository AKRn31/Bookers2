<%= render 'sidebar', book: @book %>
  <div class="offset-md-1"></div>
    <div class="col-md-8">
      <h1>Books</h1>
      <table class="table">
        <thead>
          <tr>
            <th scope="col-md-1"></th>
            <th scope="col">Title</th>
            <th scope="col-6 ">Opinion</th>
          </tr>
        </thead>
       <% @books.each do |book| %>
        <tbody>
          <tr>
            <td><%= image_tag current_user.get_profile_image(100,100) %></td>
            <td> <%= link_to book.title, book_path(book.id) %></td>
            <td><%= book.body %></td>
          </tr>　　　 
        </tdoby>  
       <% end %>
      </table>
  </div>  
</div>

  